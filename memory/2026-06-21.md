# 2026-06-21

- User removed Ollama and cleaned OpenClaw config references.
- User wants Windows startup to launch OpenClaw more reliably.
- Final preferred startup flow:
  1. Run `openclaw gateway run` in a separate PowerShell window.
  2. Wait for Gateway readiness on `127.0.0.1:18789` (up to 300 seconds).
  3. Then open a new PowerShell window for `openclaw dashboard`.
  4. Open `https://github.com/settings/copilot/features` for GitHub Copilot usage.
- Removed Evernote/印象笔记 startup shortcut from the Windows Startup folder.
- Startup launcher currently lives at:
  `C:\Users\Administrator\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\OpenClaw Startup.cmd`\n- Notable improvement: keep the startup script explicit and resilient so it can be edited later if Gateway/dashboard sequencing changes.
- Keyword shortcut for this setup: `开机启动小龙虾`.
  - When the user says this, retrieve this startup context first and continue editing from here.
  - After the startup task is complete, treat this context as archived unless the user explicitly asks to modify it again.
