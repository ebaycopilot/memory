# 2026-06-25

- Verified the OpenClaw startup launcher keeps a gateway window open with `-NoExit` and `Pause`, but discovered repeated starts could collide on the same temp log file.
- Updated the startup launcher to use a timestamped log file per run so restarts do not conflict.
- Synced the fix into the D:\github\memory repo at `startup\OpenClaw Startup.cmd` for push-upstream tracking.\n