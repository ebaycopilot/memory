# 2026-07-01

- Added a new OpenClaw startup preflight for GitHub Copilot / social-commenter token refresh.
- The startup flow now clears `GITHUB_COPILOT_TOKEN` and `SOCIAL_COMMENTER_API_KEY`, launches VS Code, waits for `C:\Users\Administrator\AppData\Local\Temp\oc-vscode-copilot-bridge-status.json` to report `stage=done` and `ok=true`, then verifies the token was written back before continuing.
- Verified the refreshed token length was 433 and the startup script emitted `token-refresh-ok` after VS Code re-authentication.
- Synced the updated startup launcher and helper script into the memory repo so the boot workflow is tracked with the rest of the durable notes.
